# generated by: yake code goreleaser
version: 2
before:
  hooks:
    - go mod tidy
builds:
  - id: mqtt-forward
    binary: mqtt-forward
    env:
      - CGO_ENABLED=0
    goos:
      - linux
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}
checksum:
  name_template: checksums.txt
  algorithm: sha256
snapshot:
  version_template: '{{ .Version }}-{{ .CommitTimestamp }}-{{ .ShortCommit }}'
changelog:
  disable: true
nfpms:
  - id: mqtt-forward
    package_name: mqtt-forward
    maintainer: vitalvas
    formats:
      - deb
    contents:
      - src: packaging/systemd/mqtt-forward.service
        dst: /lib/systemd/system/mqtt-forward.service
      - src: packaging/certs/AmazonRootCA1.pem
        dst: /etc/mqtt-forward/AmazonRootCA1.pem
        file_info:
          mode: 0644
      - dst: /etc/mqtt-forward
        type: dir
        file_info:
          mode: 0750
    scripts:
      postinstall: packaging/scripts/postinstall.sh
      preremove: packaging/scripts/preremove.sh
release:
  github:
    owner: vitalvas
    name: mqtt-forward
  draft: false
  prerelease: auto
  mode: keep-existing
