# generated by: yake code goreleaser
version: 2
before:
  hooks:
    - go mod tidy
builds:
  - id: mqtt-forward
    binary: mqtt-forward
    env:
      - CGO_ENABLED=0
    goos:
      - linux
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}
checksum:
  name_template: checksums.txt
  algorithm: sha256
snapshot:
  version_template: '{{ .Version }}-{{ .CommitTimestamp }}-{{ .ShortCommit }}'
changelog:
  disable: true
release:
  github:
    owner: vitalvas
    name: mqtt-forward
  draft: false
  prerelease: auto
  mode: keep-existing
